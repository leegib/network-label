play.i18n.langs = ["ko"]

db {
  default {
    driver   = "org.postgresql.Driver"
    url      = "jdbc:postgresql://"${network_label.db_host}"/"${network_label.db_name}
    username = ${network_label.db_username}
    password = ${network_label.db_password}
  }
}